# Handoff 2026-07-14

Date: 2026-07-14
Project: promo_web_builder
Scope: Wizard Content Section admin management feature (PRD implementation)

## Summary

Implemented the PRD (`docs/wizard-content-section-admin-management-prd-2026-07-14.md`) for admin-managed Wizard Step 2 content sections: DB schema, CRUD API, Admin Page UI, and dynamic Wizard rendering. Prior to this, `prototype/promo-wizard.js` hardcoded the 7 Step 2 sections (Header/Hero Banner/Step Bar/Content CTA/Image Text Row/Title and Description/Footer). They are now defined in the database and editable from the Admin Page.

## Completed

1. Database (`db/migrations/016_wizard_content_sections.sql`)
   - `wizard_content_sections`: draft/active/inactive/archived versioned rows, same pattern as `prompt_templates`. One active version per `section_key` enforced by a partial unique index.
   - `wizard_content_section_items`: child rows per section version. Supports `field_kind` (`text`/`image`/`cta`), `text_type` (`title`/`remark`/`multi`), image source options (`file`/`url`/`ai`) + alt-text/aspect-ratio/max-size constraints, CTA UTM fields (source/medium/campaign/content/term), and a lock mechanism (`is_locked` + `locked_value`) for admin-fixed content.
   - `wizard_content_section_histories`: change log for draft/activate/archive actions.
   - Seed data migrates the existing 7 hardcoded sections as version 1 / active, so behavior is unchanged until an admin publishes a new draft. Header/Footer logo & badge fields were kept as text (matching current behavior) rather than image, to avoid an unnecessary regression; only Content CTA's and Image Text Row's `image` items are true image fields.

2. API (`api/`)
   - `_wizard-content-sections-store.js`: shared DB helpers (row mappers, clone-as-draft, history recording).
   - `wizard-content-sections.js`: `GET` (admin list, or `?scope=public` for the Wizard-facing read with nested items), `POST` (create new section, or clone an existing one into a new draft via `{ id }`).
   - `wizard-content-section.js`: `GET` single section + items + history, `PATCH` update (draft-only).
   - `wizard-content-section-activate.js`, `wizard-content-section-archive.js`: version lifecycle actions (mirrors `prompt-template-activate.js`/`-archive.js`).
   - `wizard-content-section-items.js`: item CRUD, restricted to sections currently in `draft` status.
   - All new `api/*.js` files passed `node --check`.

3. Admin Page (`prototype/index.html` + `app.js`)
   - New "C. Wizard Content Sections 관리" subsection added below the existing A/B (Worker Webhook / Prompt) panels.
   - Section list (grouped by `sectionKey`, shows version/status), section field editor (name/description/required/order-change/visibility, draft-only), "새 초안 만들기"/"활성화"/"보관(삭제)" actions, and a nested item editor supporting all field kinds (text/image/cta), lock toggle with JSON locked-value input, and image source checkboxes.

4. Wizard (`prototype/promo-wizard.js` + `.css`)
   - `defaultSectionInputs()` (hardcoded) replaced by `defaultSectionInputsFromDefinitions()` + `mergeSectionInputs()`, driven by definitions fetched from `/api/wizard-content-sections?scope=public`.
   - `renderContentStep()` now renders sections 4+ from `wizardSectionDefinitions` instead of 7 fixed blocks; added loading/error states.
   - New field renderers: `createDynamicSectionField`, `createCtaSectionField`, `createImageSectionField`, `createLockedSectionField` (locked items render read-only).
   - `contentErrors()` / `dynamicCoverageRows()` now scan admin-defined `isRequired` items instead of a fixed 11-row checklist.
   - `buildWizardPayload()` updated to the new key names (`heroBanner.button.label` instead of `heroBanner.cta.label`, `stepBar.title` instead of `stepBar.0.title`, etc.) and now sends `sectionConfig.sections` with real `name`/`description` per section, so `api/_promo-markdown-builders.js`'s `canonicalSectionName()`/`sectionRole()` fallback maps are no longer load-bearing for Wizard-originated payloads (no backend file was changed).

## Verification

- All new `api/*.js` files: `node --check` passed.
- `prototype/promo-wizard.js`, `index.html`, `app.js`: could not run `node --check` reliably in this session — the sandbox's mount of this connected folder appeared to serve a stale/partial snapshot (`app.js`/`index.html` timestamps stuck at yesterday, `promo-wizard.js` showed a truncated mid-edit snapshot) regardless of how long we waited. Structural correctness was instead verified by re-reading every edited region directly (function/brace boundaries) and by balanced-tag counts (`<section>`/`</section>`, `<div>`/`</div>`, `<label>`/`</label>` all matched in `index.html`).
- **Recommended before merging**: run `npm run check` locally (or in an environment with a fresh mount/checkout) to get a real Node syntax pass on `app.js`, `index.html`-adjacent JS, and `promo-wizard.js`.
- **Not yet done**: the migration was not applied to any live database (no DATABASE_URL/DB access in this session), and there was no end-to-end browser test of the new Admin UI or Wizard flow.

## Notes And Issues

- Scope reduction: Step Bar and Image Text Row are no longer repeatable (previously 3 steps / N image-text cards). Each admin-defined section now holds exactly one instance per item. Supporting N-count repeatable item groups was out of scope for this pass — flagged as a follow-up.
- Image items currently accept a URL or AI-prompt text value in the Wizard; there is no real file-upload wiring to Vercel Blob yet. `image_allowed_sources` include `"file"` as an option but the Wizard UI doesn't yet provide a file picker for it.
- Admin API auth: still intentionally deferred per earlier discussion (PoC stage). The new `wizard-content-section*.js` endpoints have the same no-auth posture as the rest of `api/`.
- `docs/claude/review-promo-wizard-frontend-source-2026-07-14.md` flagged a "focus lost while typing" bug in `createField()`/`setFieldValue()`. That bug was already fixed by the time this session touched the file (no `renderStep()` call left in `setFieldValue()`, and the fragile `:nth-child(3)` selector was replaced with a `data-field-key` attribute) — fixed by another tool/session, not by this work.

## Next Recommended Work

1. Run `npm run check` + open `prototype/promo-wizard.html` and the Admin Page locally to smoke-test the new dynamic rendering end-to-end.
2. Apply `db/migrations/016_wizard_content_sections.sql` to the target database.
3. Decide on the 4 PRD open questions still relevant to file uploads: real file-upload flow for `image` items with `"file"` allowed source.
4. Consider whether Step Bar / Image Text Row need repeatable (N-count) item groups restored, or whether the single-instance simplification is acceptable long-term.

## Files To Inspect Next

```text
db/migrations/016_wizard_content_sections.sql
api/_wizard-content-sections-store.js
api/wizard-content-sections.js
api/wizard-content-section.js
api/wizard-content-section-activate.js
api/wizard-content-section-archive.js
api/wizard-content-section-items.js
prototype/index.html (admin-panel-c section)
prototype/app.js (Wizard Content Sections methods, search "wizardSection")
prototype/promo-wizard.js (search "wizardSectionDefinitions")
prototype/promo-wizard.css (.locked-field-value)
docs/wizard-content-section-admin-management-prd-2026-07-14.md
```

## Codex Hardening Update

### Applied

1. Legacy Step 2 content preservation
   - Added section input schema version 2.
   - Added a one-time localStorage backup before migration.
   - Migrates legacy Header/Hero/CTA keys to the admin-managed canonical keys.
   - Preserves legacy Step Bar/Image Text Row arrays in `legacyItems` while the repeatable-group UI remains pending.

2. Wizard fail-closed behavior
   - Step 2 cannot proceed while section definitions are loading, failed, or empty.
   - Added a configuration retry action.
   - `buildWizardPayload()` independently rejects generation without a ready configuration.
   - Public configuration revision is included in the run payload snapshot.

3. Validation completion
   - Required CTA items now require label and link.
   - Required image items validate value and required alt text.
   - Locked items are no longer assumed valid when their fixed value is missing.
   - Server validates typed locked values, supported image sources, AI prompt text, and positive image size.
   - File image sources are temporarily disabled until Blob upload is implemented.
   - Admin CTA UTM settings are appended to generated section and primary CTA URLs.

4. Atomic lifecycle
   - Migration adds PostgreSQL functions for atomic draft cloning and activation.
   - Draft versions use `max(version) + 1` under an advisory transaction lock.
   - Item cloning and lifecycle history are part of the same database transaction.
   - Active sections cannot be archived directly in either UI or API.

5. Contract coverage
   - Added `scripts/test-wizard-content-sections-contract.js`.
   - Added package script `test:wizard-content-sections-contract`.

### Verification Completed

- Node syntax passed for `prototype/app.js`, `prototype/promo-wizard.js`, all six Wizard Content Section API files, and the new contract test.
- Wizard Content Sections contract test passed.
- Integrated Brief completion contract test passed.
- Worker prompt contract test passed.
- LO-FI worker contract test passed.
- Final worker contract test passed.
- `git diff --check` passed; only existing Windows LF/CRLF warnings were reported.

### Still Pending

1. Apply and verify `016_wizard_content_sections.sql` on a test/staging Neon branch.
2. Browser E2E for Admin CRUD, activation, Wizard migration, configuration failure/retry, and Step 2 payload.
3. Full repeatable-group model and UI for Step Bar/Image Text Row. Existing arrays are preserved but only the first item is currently editable in the dynamic Wizard UI.
4. Real file upload and Vercel Blob integration. The `file` source remains disabled until then.
5. Administrator authentication/authorization. Deployment protection or a server-verified admin session is required before public production exposure.
6. Add DB integration tests for concurrent activation and rollback behavior.

### Additional Debug Result

- `scripts/test-design-prompt-md.js` passed both Design Prompt MD and Section Input Log MD checks.
- `scripts/qa-label-guard.js` still fails on pre-existing n8n workflow JSON files that are outside this Admin/Wizard change:
  - `n8n/promo-ui-design-image-generator.workflow.json`
  - `n8n/Promo UI Design Image Generator_test (2).gemini-fixed.json`
  - `n8n/Promo UI Design Image Generator_testing.gemini-http.json`
- The failure is missing prompt guard phrases and was not introduced or modified by the Wizard Content Sections implementation.

### Deployment Warning

Do not deploy the new Admin/Wizard schema directly to production before the migration succeeds on staging and the browser E2E passes. The API now expects the lifecycle SQL functions created by migration 016.

---

## Form Template L/M/R Admin Update (End Of Day)

### Objective And Final UX Direction

The Promotion Form Admin screen is being reorganized into three functional areas.

- **L (left):** form template list. The `+ 템플릿 추가` action is placed at the bottom of this list.
- **M (middle):** Sections owned by the selected template. A Section is created directly here instead of selecting a shared Section from the common library.
- **R (right):** Items belonging to the selected Section. Section field editing was removed from R; R is now reserved for Section Item management.

The meaning of `고정` was clarified during implementation:

- `user_reorder_allowed`: whether a Promo Wizard user may reorder a Section or Item.
- `fixed_position` (`top`/`bottom`): structural pinning to the top or bottom.
- Admin users may reorder draft Sections and Items regardless of `user_reorder_allowed`.
- Item `is_locked` means the content value itself is fixed and cannot be edited by a Wizard user. It is not an ordering lock.

The detailed implementation plan is in:

- `docs/claude/promo-form-template-lmr-development-plan-2026-07-14.md`

### Database And API Work Completed

1. Migration `db/migrations/018_template_owned_sections_and_reorder.sql`
   - Added `wizard_content_sections.owner_form_template_id`.
   - Added `wizard_form_template_sections.section_id` and `user_reorder_allowed`.
   - Added `wizard_content_section_items.user_reorder_allowed`.
   - Backfilled existing template relations with active Section IDs where possible.
   - Added the owned-Section activation helper.
   - **User reported that migration 018 was applied successfully on 2026-07-14.**

2. Template Section APIs
   - M can create a new template-owned draft Section (`createNew: true`).
   - Section relation settings now keep Wizard reorder permission separate from admin ordering.
   - Deleting a template-owned draft Section also deletes its items through the existing cascade.
   - Section ordering ignores Wizard reorder permission and only excludes structurally pinned Sections.

3. Section Item APIs
   - Item CRUD now reads/writes `user_reorder_allowed`.
   - Added `api/wizard-content-section-items-order.js` for draft Item ordering.
   - Neon-compatible JSONB array expansion is used for ordered Item IDs.

4. Activation
   - Activating a form template also activates its owned draft Sections through `activate_wizard_form_template_owned_sections`.

### Admin UI Work Completed

1. L area
   - Template list remains the primary selector.
   - `+ 템플릿 추가` moved to the bottom of the L list.

2. M area
   - `+ Section 추가` creates a new Section rather than importing a common-library Section.
   - Section cards continue to support drag-and-drop ordering with visible motion.
   - Section editing moved from R into an expandable area inside each M card.
   - Expand/collapse is controlled by a dedicated icon:
     - `▸`: open Section settings.
     - `▾`: close Section settings.
   - Opening a different Section closes the previously expanded Section.
   - Height, opacity, and vertical-motion transitions were added.
   - `prefers-reduced-motion` disables the animation when requested by the OS.
   - Expansion state is separate from selection state: selecting a card controls R Item loading; the icon controls the M settings panel.
   - Expansion now starts immediately and does not wait for the Item API response.

3. R area
   - Removed Section-level settings.
   - Added Item create, update, delete, and up/down ordering actions.
   - Multiple Items can be added consecutively; after save, a fresh Item form remains available.
   - Text (`title`/`remark`/`multi`), image, CTA UTM, visibility, required, Wizard reorder permission, and fixed-content settings are represented.

4. Cache control
   - Admin assets currently use `template-section-accordion-v4` in `prototype/index.html` so browsers do not mix older JS/CSS with the new markup.

### Major Issue Found Late Today: Section Save Appeared Successful But Reverted

#### Reproduction

1. Open a Section in M.
2. Change the Section name or settings and click `수정 저장`.
3. The UI reports that the Section was saved.
4. Select another template and return.
5. The Section values return to their previous state.

#### Root Cause

Draft cloning and template duplication copied the `wizard_form_template_sections` relation but could still point to a Section owned by the source/previous template. The PATCH handler only updated Section name/description when the source Section was both:

- owned by the current template, and
- in `draft` status.

For a shared/foreign Section, the Section update affected zero rows, but the handler still returned HTTP 200. The client therefore displayed a false success message.

#### Fix Applied In Commit `2454331`

`api/wizard-form-template-sections.js` now performs copy-on-edit:

1. Detects when the linked Section is owned by another template or is not a draft.
2. Creates a new current-template-owned draft Section.
3. Copies all existing Section Items, including `user_reorder_allowed`, image settings, CTA UTM fields, and locked values.
4. Repoints the template relation to the new Section.
5. Applies the requested Section name, description, and relation settings.
6. Re-reads the template Sections and returns the persisted DB representation.

Contract assertions were added to `scripts/test-wizard-form-templates-contract.js`.

**Important:** this final fix passed syntax and contract tests but was not manually re-tested in the deployed Admin UI before stopping today.

### Verification Completed Today

- `node --check prototype/app.js`: passed.
- `node --check prototype/promo-wizard.js`: passed.
- `node --check api/wizard-form-template-sections.js`: passed.
- `scripts/test-wizard-form-templates-contract.js`: passed.
- `scripts/test-wizard-content-sections-contract.js`: passed.
- `git diff --check`: passed; only Windows LF/CRLF warnings were emitted.
- Migration 018: reported complete by the user.
- Worktree at handoff creation: clean.
- Latest commit: `2454331 섹션수정--1`.

### Known Issues And Risks

1. **Final Section persistence fix needs deployed E2E verification.**
   - Confirm that `2454331` is deployed before testing.
   - A local success message alone is not sufficient; switch templates and return, then hard-refresh and verify again.

2. **Copy-on-edit is currently a multi-statement API operation.**
   - Section insert, Item copy, relation repoint, and value update are not wrapped in one database transaction.
   - A failure between statements could leave an unused owned Section or a partially copied state.
   - Recommended hardening: move this operation into one PostgreSQL function in a follow-up migration.

3. **The API still needs persisted-value validation.**
   - The server now re-reads and returns the saved Section, but the UI success path does not compare every requested field with the returned fields.
   - A mismatch should produce an explicit failure instead of a generic success message.

4. **Browser automation was unavailable in this session.**
   - The bundled Playwright package failed because `playwright-core` was missing.
   - HTTP/static checks and contract tests passed, but visual interaction was manually delegated to the user.

5. **Existing n8n QA failure remains unrelated and unresolved.**
   - `scripts/qa-label-guard.js` reports missing prompt guard phrases in three n8n workflow JSON files.
   - Five phrase checks fail in total.
   - These files were intentionally not modified during the form-template UI work.

6. **Real file upload remains pending.**
   - Image Item UI/API currently supports URL and AI sources in the active implementation.
   - A real Vercel Blob upload path is still required for the original `파일첨부` requirement.

7. **Template duplication semantics need a dedicated regression test.**
   - Copy-on-edit handles foreign/shared Sections lazily.
   - It is still necessary to decide whether duplication should eagerly deep-copy all Sections and Items at duplicate time instead.

### Tomorrow: Recommended Resume Order

#### P0. Confirm Deployment And Re-test Section Persistence

1. Verify that deployment contains commit `2454331`.
2. Open Admin Page → `프로모션 폼관리`.
3. Select a draft template and expand a Section using `▸`.
4. Change name, description, required, visibility, Wizard reorder permission, and fixed position.
5. Click `수정 저장`.
6. Select another template, return, and verify persisted values.
7. Hard-refresh the page and verify again.
8. Check that the Section's Items remain present after the first copy-on-edit save.

#### P0. Re-test Expand/Collapse UX

1. Confirm immediate animation on `▸` click even if Item loading is slow.
2. Confirm `▾` closes the panel.
3. Confirm opening another Section closes the previous panel.
4. Confirm card selection updates R without unexpectedly toggling M.
5. Confirm save preserves the expanded card and delete closes/removes it.

#### P1. Test R Item CRUD And Ordering

1. Create multiple text Items consecutively.
2. Update visibility, required, Wizard reorder permission, and fixed content.
3. Test image and CTA Items.
4. Move Items up/down and verify persistence after template switching and refresh.
5. Delete an Item and verify it does not return.

#### P1. Harden Copy-on-edit

1. Add an atomic PostgreSQL function for owned Section + Item cloning and relation repointing.
2. Add DB integration tests for rollback and repeated edits.
3. Prevent false success by comparing the persisted response with the requested values.

#### P2. Continue Wizard Integration

1. Add template selection to Wizard Step 2.
2. Load only Sections/Items from the selected active form template.
3. Apply Section/Item `user_reorder_allowed` in the Wizard reorder UI.
4. Preserve fixed-position and fixed-content behavior separately.

### Files To Start With Tomorrow

```text
api/wizard-form-template-sections.js
api/_wizard-form-templates-store.js
api/wizard-content-section-items.js
api/wizard-content-section-items-order.js
db/migrations/018_template_owned_sections_and_reorder.sql
prototype/index.html
prototype/app.js
prototype/styles.css
scripts/test-wizard-form-templates-contract.js
scripts/test-wizard-content-sections-contract.js
docs/claude/promo-form-template-lmr-development-plan-2026-07-14.md
```
